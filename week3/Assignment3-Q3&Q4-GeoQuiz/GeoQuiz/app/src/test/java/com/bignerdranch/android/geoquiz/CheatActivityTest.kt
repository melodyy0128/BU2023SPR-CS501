package com.bignerdranch.android.geoquiz

import androidx.lifecycle.SavedStateHandle
import org.junit.Assert.*
import org.junit.Test

class CheatActivityTest{
    @Test
    //rotatedStillCheating
    fun printRightAnswer() {
//        val savedStateHandle = SavedStateHandle(mapOf(EXTRA_ANSWER_SHOWN to true))
//        val cheatActivity = CheatActivity(savedStateHandle)
//        assertEquals("true", cheatActivity.cheated)

    }

}